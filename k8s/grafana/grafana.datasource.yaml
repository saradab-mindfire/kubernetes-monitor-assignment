apiVersion: v1
kind: ConfigMap

metadata:
  name: grafana-datasource
  namespace: monitoring-app

data:
  prometheus.yaml: |
    apiVersion: 1
    datasources:
      - name: Prometheus
        type: prometheus
        uid: prometheus
        url: http://prometheus-service:9090
        isDefault: true
        editable: false
