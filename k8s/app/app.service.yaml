apiVersion: v1
kind: Service

metadata:
  name: monitoring-app-service
  namespace: monitoring-app

spec:
  selector:
    app: monitoring-app

  ports:
    - port: 80
      targetPort: 3000

  type: ClusterIP
